[package]
name = "rff-resample"
description = "Remade FFmpeg (Rust): audio sample-rate conversion — the swresample equivalent."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
